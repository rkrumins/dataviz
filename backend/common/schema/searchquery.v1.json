{
  "$defs": {
    "AggregationSpec": {
      "description": "Roll matches up to ancestors (or facets) for orient-before-drill UX.\n\nPure-aggregate responses skip the result-row ordering + ancestor-\nhydration steps and are accordingly the cheapest mode the provider\ncan run. One level of ``sub_aggregation`` is permitted; deeper\ndrilling is meant to be done by re-issuing a scoped request \u2014 that\niteration is also the AI-agent facet-discovery pattern.",
      "properties": {
        "ancestorEntityTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 16,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required when by='ancestorType'.",
          "title": "Ancestorentitytypes"
        },
        "ancestorLevel": {
          "anyOf": [
            {
              "maximum": 20,
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required when by='ancestorLevel'.",
          "title": "Ancestorlevel"
        },
        "by": {
          "default": "ancestorType",
          "enum": [
            "ancestorType",
            "ancestorLevel",
            "ancestor",
            "parent",
            "tag",
            "entityType",
            "property"
          ],
          "title": "By",
          "type": "string"
        },
        "maxBuckets": {
          "default": 50,
          "description": "Bucket ceiling. The headroom above a facet-sized list is for by='ancestor', which needs one bucket per container the canvas can collapse.",
          "maximum": 20000,
          "minimum": 1,
          "title": "Maxbuckets",
          "type": "integer"
        },
        "propertyKey": {
          "anyOf": [
            {
              "maxLength": 128,
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required when by='property'. The native node property whose values become the bucket keys (e.g. 'layer' \u2192 one bucket per layer value).",
          "title": "Propertykey"
        },
        "sampleHitsPerBucket": {
          "default": 3,
          "description": "Tiny preview list shown next to each bucket \u2014 for the UI's hover-card and the AI-agent's at-a-glance context.",
          "maximum": 20,
          "minimum": 0,
          "title": "Samplehitsperbucket",
          "type": "integer"
        },
        "subAggregation": {
          "anyOf": [
            {
              "$ref": "#/$defs/AggregationSpec"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "One-level nested drill. Deeper levels require a follow-up scoped search (see module docstring)."
        }
      },
      "title": "AggregationSpec",
      "type": "object"
    },
    "AncestorRef": {
      "description": "Compact ancestor identifier \u2014 enough for the FE to render a\nbreadcrumb without re-fetching the node.",
      "properties": {
        "displayName": {
          "title": "Displayname",
          "type": "string"
        },
        "entityType": {
          "title": "Entitytype",
          "type": "string"
        },
        "urn": {
          "title": "Urn",
          "type": "string"
        }
      },
      "required": [
        "urn",
        "displayName",
        "entityType"
      ],
      "title": "AncestorRef",
      "type": "object"
    },
    "DegreePredicate": {
      "description": "Match nodes by their edge degree.\n\nPowers structural questions like \"find datasets with zero incoming\nlineage edges\" (UC-1 orphan finder), \"find tables with > 5 children\",\nor \"find views that produce >= 1 downstream artifact\".\n\nThe edge type set is resolved from the live ontology at compile time\nvia the provider's ``_get_lineage_edge_types`` /\n``_get_containment_edge_types`` helpers, so no hardcoded names ever\nleak into Cypher. ``edge_types`` lets a caller pin a specific subset\n(e.g. ``['PRODUCES']`` only); when omitted, the ``edge_class`` chooses\nthe default set.",
      "properties": {
        "direction": {
          "default": "both",
          "enum": [
            "in",
            "out",
            "both"
          ],
          "title": "Direction",
          "type": "string"
        },
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional explicit edge-type list. When provided, overrides the default set resolved from ``edge_class``.",
          "title": "Edgetypes"
        },
        "kind": {
          "const": "degree",
          "default": "degree",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "eq",
          "enum": [
            "eq",
            "neq",
            "gt",
            "gte",
            "lt",
            "lte"
          ],
          "title": "Op",
          "type": "string"
        },
        "value": {
          "maximum": 1000000,
          "minimum": 0,
          "title": "Value",
          "type": "integer"
        }
      },
      "required": [
        "value"
      ],
      "title": "DegreePredicate",
      "type": "object"
    },
    "DescendantOfPredicate": {
      "description": "Clamp matches to the subtree(s) rooted at ``urns``.\n\nLives as a predicate (not on ``SearchScope``) so it can sit inside\nOR groups \u2014 e.g. ``(under Customers OR under Orders) AND tag=PII``.\nThe single-subtree case is also expressible via ``scope.root_urns``,\nwhich the provider may push down as a tighter Cypher anchor.",
      "properties": {
        "kind": {
          "const": "descendantOf",
          "default": "descendantOf",
          "title": "Kind",
          "type": "string"
        },
        "maxDepth": {
          "anyOf": [
            {
              "maximum": 20,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Maxdepth"
        },
        "urns": {
          "items": {
            "type": "string"
          },
          "maxItems": 64,
          "minItems": 1,
          "title": "Urns",
          "type": "array"
        }
      },
      "required": [
        "urns"
      ],
      "title": "DescendantOfPredicate",
      "type": "object"
    },
    "EdgeGroupPredicate": {
      "description": "Boolean composition of edge predicates.\n\nSame shape as ``GroupPredicate`` but scoped to a traversed edge.\nService-layer validator enforces depth / leaf caps mirroring the\nnode-predicate tree.",
      "properties": {
        "children": {
          "items": {
            "discriminator": {
              "mapping": {
                "edgeGroup": "#/$defs/EdgeGroupPredicate",
                "edgeHasProperty": "#/$defs/EdgeHasPropertyPredicate",
                "edgeProperty": "#/$defs/EdgePropertyPredicate"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/EdgePropertyPredicate"
              },
              {
                "$ref": "#/$defs/EdgeHasPropertyPredicate"
              },
              {
                "$ref": "#/$defs/EdgeGroupPredicate"
              }
            ]
          },
          "maxItems": 32,
          "minItems": 1,
          "title": "Children",
          "type": "array"
        },
        "kind": {
          "const": "edgeGroup",
          "default": "edgeGroup",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "and",
          "enum": [
            "and",
            "or",
            "not"
          ],
          "title": "Op",
          "type": "string"
        }
      },
      "required": [
        "children"
      ],
      "title": "EdgeGroupPredicate",
      "type": "object"
    },
    "EdgeHasPropertyPredicate": {
      "description": "Key-presence predicate against an edge. Compiles to\n``EXISTS(rel.<key>)`` (or ``NOT EXISTS`` when ``negate``).",
      "properties": {
        "key": {
          "maxLength": 128,
          "minLength": 1,
          "title": "Key",
          "type": "string"
        },
        "kind": {
          "const": "edgeHasProperty",
          "default": "edgeHasProperty",
          "title": "Kind",
          "type": "string"
        },
        "negate": {
          "default": false,
          "title": "Negate",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "title": "EdgeHasPropertyPredicate",
      "type": "object"
    },
    "EdgePropertyPredicate": {
      "description": "Typed comparison against a single edge property.\n\nEvaluated against each traversed relationship inside a\n``PathPredicate`` or ``WithinHopsPredicate``. Compiles to\n``rel.<key> <op> $val`` inside an ``ALL(rel IN relationships(p) \u2026)``\nblock. ``between`` expects ``value`` to be a two-element list\n``[lo, hi]``.",
      "properties": {
        "key": {
          "maxLength": 128,
          "minLength": 1,
          "title": "Key",
          "type": "string"
        },
        "kind": {
          "const": "edgeProperty",
          "default": "edgeProperty",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "eq",
          "enum": [
            "eq",
            "neq",
            "gt",
            "gte",
            "lt",
            "lte",
            "in",
            "notIn",
            "contains",
            "startsWith",
            "endsWith",
            "between"
          ],
          "title": "Op",
          "type": "string"
        },
        "value": {
          "default": null,
          "title": "Value"
        }
      },
      "required": [
        "key"
      ],
      "title": "EdgePropertyPredicate",
      "type": "object"
    },
    "EdgeRef": {
      "description": "One edge in a returned path. Carries enough for the FE to render\na directed arrow between two nodes plus expose edge properties on\nhover. We don't return a full GraphEdge to keep the path response\ncompact \u2014 the provider knows about the relationship type and\nendpoints, which is what callers actually render.",
      "properties": {
        "edgeType": {
          "title": "Edgetype",
          "type": "string"
        },
        "properties": {
          "additionalProperties": true,
          "title": "Properties",
          "type": "object"
        },
        "sourceUrn": {
          "title": "Sourceurn",
          "type": "string"
        },
        "targetUrn": {
          "title": "Targeturn",
          "type": "string"
        }
      },
      "required": [
        "sourceUrn",
        "targetUrn",
        "edgeType"
      ],
      "title": "EdgeRef",
      "type": "object"
    },
    "EntityTypePredicate": {
      "description": "Restrict matches to (or away from) specific entity types.\n\nEquivalent expressivity to ``scope.entity_types``, but composable\ninside OR groups.",
      "properties": {
        "kind": {
          "const": "entityType",
          "default": "entityType",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "in",
          "enum": [
            "in",
            "notIn"
          ],
          "title": "Op",
          "type": "string"
        },
        "values": {
          "items": {
            "type": "string"
          },
          "maxItems": 32,
          "minItems": 1,
          "title": "Values",
          "type": "array"
        }
      },
      "required": [
        "values"
      ],
      "title": "EntityTypePredicate",
      "type": "object"
    },
    "GraphNode": {
      "properties": {
        "childCount": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Childcount"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "displayName": {
          "title": "Displayname",
          "type": "string"
        },
        "entityType": {
          "title": "Entitytype",
          "type": "string"
        },
        "lastSyncedAt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Lastsyncedat"
        },
        "layerAssignment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Layerassignment"
        },
        "properties": {
          "additionalProperties": true,
          "title": "Properties",
          "type": "object"
        },
        "qualifiedName": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Qualifiedname"
        },
        "sourceSystem": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sourcesystem"
        },
        "tags": {
          "items": {
            "type": "string"
          },
          "title": "Tags",
          "type": "array"
        },
        "urn": {
          "title": "Urn",
          "type": "string"
        },
        "version": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Version"
        }
      },
      "required": [
        "urn",
        "entityType",
        "displayName"
      ],
      "title": "GraphNode",
      "type": "object"
    },
    "GroupPredicate": {
      "description": "Boolean composition of child predicates.\n\n``op='not'`` must have exactly one child \u2014 the service-layer validator\nenforces that (the model accepts \u22651 to keep the schema simple). The\nsame validator enforces ``max_depth \u2264 6`` and ``leaf_count \u2264 64``.",
      "properties": {
        "children": {
          "items": {
            "discriminator": {
              "mapping": {
                "degree": "#/$defs/DegreePredicate",
                "descendantOf": "#/$defs/DescendantOfPredicate",
                "entityType": "#/$defs/EntityTypePredicate",
                "group": "#/$defs/GroupPredicate",
                "hasIncoming": "#/$defs/HasIncomingPredicate",
                "hasOutgoing": "#/$defs/HasOutgoingPredicate",
                "hasProperty": "#/$defs/HasPropertyPredicate",
                "isLeaf": "#/$defs/IsLeafPredicate",
                "isOrphan": "#/$defs/IsOrphanPredicate",
                "isRoot": "#/$defs/IsRootPredicate",
                "layer": "#/$defs/LayerPredicate",
                "path": "#/$defs/PathPredicate",
                "property": "#/$defs/PropertyPredicate",
                "tag": "#/$defs/TagPredicate",
                "text": "#/$defs/TextPredicate",
                "withinHops": "#/$defs/WithinHopsPredicate"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/TextPredicate"
              },
              {
                "$ref": "#/$defs/PropertyPredicate"
              },
              {
                "$ref": "#/$defs/TagPredicate"
              },
              {
                "$ref": "#/$defs/HasPropertyPredicate"
              },
              {
                "$ref": "#/$defs/DescendantOfPredicate"
              },
              {
                "$ref": "#/$defs/WithinHopsPredicate"
              },
              {
                "$ref": "#/$defs/EntityTypePredicate"
              },
              {
                "$ref": "#/$defs/LayerPredicate"
              },
              {
                "$ref": "#/$defs/DegreePredicate"
              },
              {
                "$ref": "#/$defs/IsOrphanPredicate"
              },
              {
                "$ref": "#/$defs/IsLeafPredicate"
              },
              {
                "$ref": "#/$defs/IsRootPredicate"
              },
              {
                "$ref": "#/$defs/HasIncomingPredicate"
              },
              {
                "$ref": "#/$defs/HasOutgoingPredicate"
              },
              {
                "$ref": "#/$defs/PathPredicate"
              },
              {
                "$ref": "#/$defs/GroupPredicate"
              }
            ]
          },
          "maxItems": 128,
          "minItems": 1,
          "title": "Children",
          "type": "array"
        },
        "kind": {
          "const": "group",
          "default": "group",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "and",
          "enum": [
            "and",
            "or",
            "not"
          ],
          "title": "Op",
          "type": "string"
        }
      },
      "required": [
        "children"
      ],
      "title": "GroupPredicate",
      "type": "object"
    },
    "HasIncomingPredicate": {
      "description": "Sugar: nodes with at least one incoming edge of the given class.\nNormalises to ``DegreePredicate(direction='in', op='gt', value=0)``.",
      "properties": {
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "kind": {
          "const": "hasIncoming",
          "default": "hasIncoming",
          "title": "Kind",
          "type": "string"
        }
      },
      "title": "HasIncomingPredicate",
      "type": "object"
    },
    "HasOutgoingPredicate": {
      "description": "Sugar: nodes with at least one outgoing edge of the given class.\nNormalises to ``DegreePredicate(direction='out', op='gt', value=0)``.",
      "properties": {
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "kind": {
          "const": "hasOutgoing",
          "default": "hasOutgoing",
          "title": "Kind",
          "type": "string"
        }
      },
      "title": "HasOutgoingPredicate",
      "type": "object"
    },
    "HasPropertyPredicate": {
      "description": "\"Does this node have a property named X\" \u2014 key presence only.\n\nCompiles to ``EXISTS(n.<key>)`` (or ``NOT EXISTS`` when ``negate``).\nNative-property storage makes this cheap; pre-refactor this required\nparsing the blob in Python for every node.",
      "properties": {
        "key": {
          "maxLength": 128,
          "minLength": 1,
          "title": "Key",
          "type": "string"
        },
        "kind": {
          "const": "hasProperty",
          "default": "hasProperty",
          "title": "Kind",
          "type": "string"
        },
        "negate": {
          "default": false,
          "title": "Negate",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "title": "HasPropertyPredicate",
      "type": "object"
    },
    "IsLeafPredicate": {
      "description": "Sugar: nodes with zero outgoing edges of the given class\n(\"dead ends\"). Normalises to\n``DegreePredicate(direction='out', op='eq', value=0)``.",
      "properties": {
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "kind": {
          "const": "isLeaf",
          "default": "isLeaf",
          "title": "Kind",
          "type": "string"
        }
      },
      "title": "IsLeafPredicate",
      "type": "object"
    },
    "IsOrphanPredicate": {
      "description": "Sugar: nodes with zero edges of the given class. Normalises to\n``DegreePredicate(direction='both', op='eq', value=0)``.",
      "properties": {
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "kind": {
          "const": "isOrphan",
          "default": "isOrphan",
          "title": "Kind",
          "type": "string"
        }
      },
      "title": "IsOrphanPredicate",
      "type": "object"
    },
    "IsRootPredicate": {
      "description": "Sugar: nodes with zero incoming edges of the given class\n(\"nobody produces this\"). Normalises to\n``DegreePredicate(direction='in', op='eq', value=0)``.",
      "properties": {
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "kind": {
          "const": "isRoot",
          "default": "isRoot",
          "title": "Kind",
          "type": "string"
        }
      },
      "title": "IsRootPredicate",
      "type": "object"
    },
    "LayerPredicate": {
      "description": "Match the view's layer assignment (Source / Staging / Refinery / \u2026).",
      "properties": {
        "kind": {
          "const": "layer",
          "default": "layer",
          "title": "Kind",
          "type": "string"
        },
        "layerAssignment": {
          "minLength": 1,
          "title": "Layerassignment",
          "type": "string"
        }
      },
      "required": [
        "layerAssignment"
      ],
      "title": "LayerPredicate",
      "type": "object"
    },
    "PathHit": {
      "description": "One returned path: an ordered alternating sequence of nodes and\nedges, with the hop count for sorting / display.\n\nConstraint: ``len(nodes) == len(edges) + 1`` always. The first\nnode is one of the ``source_urns`` and the last is one of the\n``target_urns`` (after direction normalisation).",
      "properties": {
        "edges": {
          "items": {
            "$ref": "#/$defs/EdgeRef"
          },
          "title": "Edges",
          "type": "array"
        },
        "hopCount": {
          "title": "Hopcount",
          "type": "integer"
        },
        "nodes": {
          "description": "Ordered list \u2014 first is source endpoint, last is target endpoint.",
          "items": {
            "$ref": "#/$defs/AncestorRef"
          },
          "title": "Nodes",
          "type": "array"
        }
      },
      "required": [
        "nodes",
        "edges",
        "hopCount"
      ],
      "title": "PathHit",
      "type": "object"
    },
    "PathPredicate": {
      "description": "Find paths between source and target nodes.\n\nTop-level AND only \u2014 the compiler raises CompileError if it\nappears inside an OR or NOT group. When present, ``options.results``\nmust be ``'paths'``; the response carries ordered node\u2192edge\u2192node\nsequences instead of (or in addition to) flat hits / aggregates.\n\nPowers UC3: \"find all paths from dataset:orders to dataset:reporting\ngoing through \u22644 lineage hops\".",
      "properties": {
        "direction": {
          "default": "outgoing",
          "description": "``outgoing`` walks source \u2192 target along edge direction; ``incoming`` walks against direction; ``any`` is undirected.",
          "enum": [
            "outgoing",
            "incoming",
            "any"
          ],
          "title": "Direction",
          "type": "string"
        },
        "edgeClass": {
          "default": "lineage",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgePredicate": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "edgeGroup": "#/$defs/EdgeGroupPredicate",
                  "edgeHasProperty": "#/$defs/EdgeHasPropertyPredicate",
                  "edgeProperty": "#/$defs/EdgePropertyPredicate"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/EdgePropertyPredicate"
                },
                {
                  "$ref": "#/$defs/EdgeHasPropertyPredicate"
                },
                {
                  "$ref": "#/$defs/EdgeGroupPredicate"
                }
              ]
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional predicate evaluated against every edge in the returned paths. ANDs with ``edge_types`` / ``edge_class``. Compiles to ``ALL(rel IN relationships(p) WHERE \u2026)``.",
          "title": "Edgepredicate"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 32,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional explicit edge-type list; overrides ``edge_class``'s resolved set.",
          "title": "Edgetypes"
        },
        "kind": {
          "const": "path",
          "default": "path",
          "title": "Kind",
          "type": "string"
        },
        "maxHops": {
          "default": 4,
          "description": "Maximum path length in edge hops.",
          "maximum": 6,
          "minimum": 1,
          "title": "Maxhops",
          "type": "integer"
        },
        "maxPaths": {
          "default": 32,
          "description": "Hard cap on the number of paths returned.",
          "maximum": 128,
          "minimum": 1,
          "title": "Maxpaths",
          "type": "integer"
        },
        "sourceUrns": {
          "description": "Starting nodes. The provider runs a variable-length match outward (or inward) from each.",
          "items": {
            "type": "string"
          },
          "maxItems": 8,
          "minItems": 1,
          "title": "Sourceurns",
          "type": "array"
        },
        "targetUrns": {
          "description": "Endpoint nodes. Paths terminate here.",
          "items": {
            "type": "string"
          },
          "maxItems": 8,
          "minItems": 1,
          "title": "Targeturns",
          "type": "array"
        }
      },
      "required": [
        "sourceUrns",
        "targetUrns"
      ],
      "title": "PathPredicate",
      "type": "object"
    },
    "PropertyPredicate": {
      "description": "Typed comparison against a single user-property.\n\nAfter the storage refactor, user properties are native FalkorDB\nfields, so these compile to ``WHERE n.<key> <op> $val`` \u2014 no Python\npost-filter. ``eq``/``neq`` case-fold (``toLower(toString(n.<key>))\n<op> toLower($val)``) when ``value`` is a string and\n``case_sensitive`` is false; a non-string value (or\n``case_sensitive=True``) keeps the raw, indexed column comparison.\n``between`` expects ``value`` to be a two-element list ``[lo, hi]``.",
      "properties": {
        "caseSensitive": {
          "default": false,
          "title": "Casesensitive",
          "type": "boolean"
        },
        "key": {
          "maxLength": 128,
          "minLength": 1,
          "title": "Key",
          "type": "string"
        },
        "kind": {
          "const": "property",
          "default": "property",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "eq",
          "enum": [
            "eq",
            "neq",
            "gt",
            "gte",
            "lt",
            "lte",
            "in",
            "notIn",
            "contains",
            "startsWith",
            "endsWith",
            "between"
          ],
          "title": "Op",
          "type": "string"
        },
        "value": {
          "default": null,
          "title": "Value"
        }
      },
      "required": [
        "key"
      ],
      "title": "PropertyPredicate",
      "type": "object"
    },
    "QueryExplain": {
      "description": "Compiled-query metadata. Returned by POST /search/explain (dry-run)\nand optionally inlined on the main search response when the caller\nasked for it. Useful for support + the FE's 'show generated query'\ntoggle in the Advanced panel.",
      "properties": {
        "costScore": {
          "default": 0.0,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Costscore",
          "type": "number"
        },
        "cypher": {
          "title": "Cypher",
          "type": "string"
        },
        "estimatedRows": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Estimatedrows"
        },
        "notes": {
          "description": "Diagnostic hints \u2014 e.g. 'no index on properties.foo'.",
          "items": {
            "type": "string"
          },
          "title": "Notes",
          "type": "array"
        }
      },
      "required": [
        "cypher"
      ],
      "title": "QueryExplain",
      "type": "object"
    },
    "ScopeDiagnostics": {
      "description": "Resolved-scope information returned on every search response.\n\nSurfaces the values the server actually applied \u2014 both the\n``ViewScopeResolver``'s output and the ontology-resolved edge type\nsets. Lets the FE explain zero-result responses (\"your view scope\nresolved to no roots\", \"your ontology has no lineage edges\")\nwithout the user having to read the compiled Cypher.\n\nNone of the fields here change query semantics; this is pure\ninstrumentation. The cost is one extra dict per response.",
      "properties": {
        "containmentEdgeTypes": {
          "description": "Edge types the active ontology flags as containment. Empty list means the graph is flat (no hierarchy) and ancestor-aware features (aggregation by='ancestorType', DescendantOf scope hoisting) won't produce useful results.",
          "items": {
            "type": "string"
          },
          "title": "Containmentedgetypes",
          "type": "array"
        },
        "droppedRootUrns": {
          "description": "Client-supplied URNs that were filtered out as out-of-view. Mirrors the X-Search-Dropped-URNs header for client convenience.",
          "items": {
            "type": "string"
          },
          "title": "Droppedrooturns",
          "type": "array"
        },
        "effectiveEntityTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Entity-type allow-list applied by the view. None means no constraint from the view.",
          "title": "Effectiveentitytypes"
        },
        "effectiveMaxDepth": {
          "title": "Effectivemaxdepth",
          "type": "integer"
        },
        "effectiveRootUrns": {
          "description": "Root URNs the scope continuation actually anchored on. Empty list means no scope clamp was applied \u2014 search ran across the whole data source.",
          "items": {
            "type": "string"
          },
          "title": "Effectiverooturns",
          "type": "array"
        },
        "lineageEdgeTypes": {
          "description": "Edge types the active ontology flags as is_lineage. Empty list means lineage-aware predicates (IsOrphan, HasIncoming, withinHops with edgeClass='lineage', etc.) will match zero nodes by design \u2014 configure at least one edge type as is_lineage in the data source's ontology.",
          "items": {
            "type": "string"
          },
          "title": "Lineageedgetypes",
          "type": "array"
        },
        "notes": {
          "description": "Human-readable diagnostic notes \u2014 e.g. 'view has no rootUrns configured; search ran unclamped'.",
          "items": {
            "type": "string"
          },
          "title": "Notes",
          "type": "array"
        }
      },
      "required": [
        "effectiveMaxDepth"
      ],
      "title": "ScopeDiagnostics",
      "type": "object"
    },
    "SearchAggregateBucket": {
      "description": "One ancestor (or facet) with N matches inside it.",
      "properties": {
        "ancestorDepthFromScopeRoot": {
          "title": "Ancestordepthfromscoperoot",
          "type": "integer"
        },
        "ancestorDisplayName": {
          "title": "Ancestordisplayname",
          "type": "string"
        },
        "ancestorEntityType": {
          "title": "Ancestorentitytype",
          "type": "string"
        },
        "ancestorUrn": {
          "title": "Ancestorurn",
          "type": "string"
        },
        "matchCount": {
          "title": "Matchcount",
          "type": "integer"
        },
        "sampleHits": {
          "items": {
            "$ref": "#/$defs/SearchHit"
          },
          "title": "Samplehits",
          "type": "array"
        },
        "subBuckets": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/SearchAggregateBucket"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Populated when the request's AggregationSpec carried a sub_aggregation.",
          "title": "Subbuckets"
        },
        "typeCounts": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "integer"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-entity-type breakdown of ``match_count`` \u2014 e.g. ``{'Column': 12, 'Table': 3}``. Populated by by='ancestor'; None for the kinds that don't compute one.",
          "title": "Typecounts"
        }
      },
      "required": [
        "ancestorUrn",
        "ancestorDisplayName",
        "ancestorEntityType",
        "ancestorDepthFromScopeRoot",
        "matchCount"
      ],
      "title": "SearchAggregateBucket",
      "type": "object"
    },
    "SearchDiscoverEdgeInfo": {
      "description": "Per-edge-type discovery payload from ``GET /search/discover``.",
      "properties": {
        "keys": {
          "description": "Property keys found on at least one sampled edge.",
          "items": {
            "type": "string"
          },
          "title": "Keys",
          "type": "array"
        },
        "sampled": {
          "description": "How many edges of this type were sampled.",
          "title": "Sampled",
          "type": "integer"
        },
        "valueSamplesByKey": {
          "anyOf": [
            {
              "additionalProperties": {
                "items": {},
                "type": "array"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Distinct values per edge property key. Powers the edge-predicate editor's value autocomplete in W2.",
          "title": "Valuesamplesbykey"
        }
      },
      "required": [
        "sampled"
      ],
      "title": "SearchDiscoverEdgeInfo",
      "type": "object"
    },
    "SearchDiscoverLabelInfo": {
      "description": "Per-label discovery payload from ``GET /search/discover``.",
      "properties": {
        "keys": {
          "description": "Native property keys found on at least one sampled node, capped at the top N by frequency in the sample (see ``truncatedProperties``).",
          "items": {
            "type": "string"
          },
          "title": "Keys",
          "type": "array"
        },
        "sampled": {
          "description": "How many nodes were sampled for this label.",
          "title": "Sampled",
          "type": "integer"
        },
        "truncatedProperties": {
          "default": false,
          "description": "True when the sample yielded more property keys than the configured per-label cap (DEEP_SEARCH_DISCOVER_KEY_CAP). FE shows a 'X of ~N properties \u2014 narrow the sample to see rare keys' hint.",
          "title": "Truncatedproperties",
          "type": "boolean"
        },
        "valueSamplesByKey": {
          "anyOf": [
            {
              "additionalProperties": {
                "items": {},
                "type": "array"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Distinct values seen for each property key in the sample (capped at ~20 per key, ~64 keys per label). Powers the FE's property-value picker so users can choose from known values instead of typing blind. Absent when value-sample collection is disabled via the request flag.",
          "title": "Valuesamplesbykey"
        }
      },
      "required": [
        "sampled"
      ],
      "title": "SearchDiscoverLabelInfo",
      "type": "object"
    },
    "SearchDiscoverResult": {
      "description": "Response shape for ``GET /search/discover``.\n\nTells the FE/AI-agent what's actually queryable in the current view:\nwhich entity-type labels exist, which native property keys + value\nsamples are present on them, what tag values exist across the\nsample, and what edge types + properties traversal can filter on.\nUsed to populate every autocomplete picker in the visual builder.",
      "properties": {
        "blobOnlyLabels": {
          "description": "Labels with a sampled node still carrying the pre-W1 `n.properties` JSON blob; those values stay invisible to property predicates until the native-property migration runs.",
          "items": {
            "type": "string"
          },
          "title": "Blobonlylabels",
          "type": "array"
        },
        "edges": {
          "additionalProperties": {
            "$ref": "#/$defs/SearchDiscoverEdgeInfo"
          },
          "description": "Per-edge-type discovery payload. Mirrors `labels` but for relationships \u2014 surfaces the edge types present in the sample plus the property keys + value samples each one carries. Powers the W2 edge-predicate editor and the edge-aware path-query value pickers.",
          "title": "Edges",
          "type": "object"
        },
        "elapsedMs": {
          "default": 0,
          "description": "Discovery query duration in milliseconds.",
          "title": "Elapsedms",
          "type": "integer"
        },
        "labels": {
          "additionalProperties": {
            "$ref": "#/$defs/SearchDiscoverLabelInfo"
          },
          "title": "Labels",
          "type": "object"
        },
        "missingContainment": {
          "default": false,
          "description": "True when the provider has no containment edge types configured \u2014 ancestor-based queries will return empty.",
          "title": "Missingcontainment",
          "type": "boolean"
        },
        "missingSearchableText": {
          "default": 0,
          "description": "Sampled nodes with no n.searchableText \u2014 run `python -m backend.scripts.migrate_native_properties --searchable-text`.",
          "title": "Missingsearchabletext",
          "type": "integer"
        },
        "tagValues": {
          "additionalProperties": {
            "type": "integer"
          },
          "description": "Map of tag name \u2192 occurrence count across the sample. Tags live as JSON-stringified arrays on `n.tags` in v1 (graph-relationship normalisation is deferred); the discovery handler parses them in Python and aggregates counts. Trimmed to the top N by count.",
          "title": "Tagvalues",
          "type": "object"
        }
      },
      "title": "SearchDiscoverResult",
      "type": "object"
    },
    "SearchExplainResult": {
      "description": "Response shape for ``POST /search/explain``.\n\nMirrors what ``backend.app.providers.falkordb_deep_search.explain_deep_search``\nreturns, plus the ``resolvedScope`` block added by the service layer.\nUsed by the FE's \"Show Cypher\" surface and by AI agents that want to\ninspect what would run before committing.",
      "properties": {
        "candidate_cap": {
          "description": "Hard cap on the candidate set the provider walks.",
          "title": "Candidate Cap",
          "type": "integer"
        },
        "cypher": {
          "description": "Full candidate Cypher (ends with `WITH n`).",
          "title": "Cypher",
          "type": "string"
        },
        "effective_root_urns": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Final root URNs after intersecting scope + hoisted (null = no scope).",
          "title": "Effective Root Urns"
        },
        "hits_cypher": {
          "description": "`cypher` plus `RETURN n` \u2014 what a hits query would actually run.",
          "title": "Hits Cypher",
          "type": "string"
        },
        "hoisted_root_urns": {
          "description": "URN sets hoisted out of top-level DescendantOf predicates.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "title": "Hoisted Root Urns",
          "type": "array"
        },
        "notes": {
          "description": "Human-readable diagnostics (e.g. 'predicate hoisted to candidate seed').",
          "items": {
            "type": "string"
          },
          "title": "Notes",
          "type": "array"
        },
        "params": {
          "additionalProperties": true,
          "description": "Bound parameters, keyed by the parameter name used in ``cypher``.",
          "title": "Params",
          "type": "object"
        },
        "resolvedScope": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Effective scope after ViewScopeResolver: root URNs, max depth, entity-type allow-list, scope hash, dropped URNs.",
          "title": "Resolvedscope"
        }
      },
      "required": [
        "cypher",
        "hits_cypher",
        "candidate_cap"
      ],
      "title": "SearchExplainResult",
      "type": "object"
    },
    "SearchHighlight": {
      "description": "Where in a hit's text the match landed, for ``<mark>`` rendering.",
      "properties": {
        "field": {
          "title": "Field",
          "type": "string"
        },
        "ranges": {
          "description": "``[start, end]`` offsets within ``snippet`` (not within the original field) \u2014 the snippet's leading ellipsis is already counted.",
          "items": {
            "items": {
              "type": "integer"
            },
            "type": "array"
          },
          "title": "Ranges",
          "type": "array"
        },
        "score": {
          "default": 0.0,
          "title": "Score",
          "type": "number"
        },
        "snippet": {
          "title": "Snippet",
          "type": "string"
        }
      },
      "required": [
        "field",
        "snippet"
      ],
      "title": "SearchHighlight",
      "type": "object"
    },
    "SearchHit": {
      "description": "One matched node, optionally with provenance.\n\n``matched_predicates`` is the list of leaf-predicate indices (0-indexed\nDFS over the request's predicate tree) that this node satisfied \u2014\nenables the FE to show \"matched on: name, logicalType\" badges.",
      "properties": {
        "ancestorPath": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/AncestorRef"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Root \u2192 parent (excluding the hit itself). Populated when options.include_ancestor_path is true.",
          "title": "Ancestorpath"
        },
        "highlights": {
          "items": {
            "$ref": "#/$defs/SearchHighlight"
          },
          "title": "Highlights",
          "type": "array"
        },
        "matchedPredicates": {
          "items": {
            "type": "integer"
          },
          "title": "Matchedpredicates",
          "type": "array"
        },
        "node": {
          "$ref": "#/$defs/GraphNode"
        },
        "score": {
          "default": 0.0,
          "title": "Score",
          "type": "number"
        }
      },
      "required": [
        "node"
      ],
      "title": "SearchHit",
      "type": "object"
    },
    "SearchOptions": {
      "description": "Per-request shape / pagination / deadline controls.\n\nDefaults are tuned for the UI's Map-mode-first experience:\n``results='aggregates'`` returns just buckets, ``page_size=50`` is\nused only when hits are requested, and a 3-second soft deadline\nkeeps the UI responsive (partial results returned on timeout).",
      "properties": {
        "aggregations": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/AggregationSpec"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Parallel facets \u2014 each spec produces its own bucket list in the response. Omit for hits-only requests.",
          "title": "Aggregations"
        },
        "candidateCap": {
          "anyOf": [
            {
              "maximum": 100000,
              "minimum": 100,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-request override of the candidate-scan ceiling. ``None`` uses the deployment default from ``DEEP_SEARCH_CANDIDATE_CAP`` (default 10000). Requests can raise this up to ``DEEP_SEARCH_CANDIDATE_CAP_MAX`` (default 100000) when the user explicitly opts into a larger scan. The service validator rejects values above the deployment max.",
          "title": "Candidatecap"
        },
        "cursor": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opaque pagination cursor. When set, hits start from the cursor's recorded offset within the candidate set. The response echoes a new cursor when more rows are available (``hits.length == pageSize`` AND the slice didn't exhaust the candidate set).",
          "title": "Cursor"
        },
        "highlights": {
          "default": true,
          "title": "Highlights",
          "type": "boolean"
        },
        "includeAncestorPath": {
          "default": false,
          "title": "Includeancestorpath",
          "type": "boolean"
        },
        "pageSize": {
          "default": 50,
          "description": "Number of hits returned per page. Default 50 (sane for browsing); the panel sets it to the candidate cap (5000) when it wants the full match set in one round-trip so canvas highlighting can cover every match without paginating. Bounded at 5000 (matches CANDIDATE_CAP) \u2014 larger pages require cursor pagination.",
          "maximum": 5000,
          "minimum": 1,
          "title": "Pagesize",
          "type": "integer"
        },
        "results": {
          "default": "aggregates",
          "enum": [
            "aggregates",
            "hits",
            "both",
            "paths"
          ],
          "title": "Results",
          "type": "string"
        },
        "softDeadlineMs": {
          "default": 30000,
          "description": "Provider returns partial rows + deadline_exceeded=true on expiry. Service does not cache deadline-exceeded responses. Default 30s (was 3s) so deep queries on large graphs complete; user can override per-request up to 120s.",
          "maximum": 120000,
          "minimum": 200,
          "title": "Softdeadlinems",
          "type": "integer"
        },
        "sort": {
          "default": "relevance",
          "enum": [
            "relevance",
            "displayName",
            "qualifiedName",
            "depth",
            "matchCount"
          ],
          "title": "Sort",
          "type": "string"
        },
        "sortDir": {
          "default": "desc",
          "enum": [
            "asc",
            "desc"
          ],
          "title": "Sortdir",
          "type": "string"
        },
        "sortProperty": {
          "anyOf": [
            {
              "maxLength": 128,
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When set, hits are ordered by this native node property (e.g. 'rowCount') instead of by `sort`. Useful for 'biggest first' / 'newest first' UX.",
          "title": "Sortproperty"
        }
      },
      "title": "SearchOptions",
      "type": "object"
    },
    "SearchQuery": {
      "description": "The request body for POST /search/advanced.\n\n``scope`` is required \u2014 every search must be bound to a view via\n``scope.view_id``. There is no global / cross-view default.\n\n``schema_version`` carries the wire-format version. Clients omit it\non outgoing requests (the default fills it in); the server echoes it\non every response so clients can fail loud on a mismatch.",
      "properties": {
        "$schemaVersion": {
          "const": "1",
          "default": "1",
          "title": "$Schemaversion",
          "type": "string"
        },
        "options": {
          "$ref": "#/$defs/SearchOptions"
        },
        "predicate": {
          "discriminator": {
            "mapping": {
              "degree": "#/$defs/DegreePredicate",
              "descendantOf": "#/$defs/DescendantOfPredicate",
              "entityType": "#/$defs/EntityTypePredicate",
              "group": "#/$defs/GroupPredicate",
              "hasIncoming": "#/$defs/HasIncomingPredicate",
              "hasOutgoing": "#/$defs/HasOutgoingPredicate",
              "hasProperty": "#/$defs/HasPropertyPredicate",
              "isLeaf": "#/$defs/IsLeafPredicate",
              "isOrphan": "#/$defs/IsOrphanPredicate",
              "isRoot": "#/$defs/IsRootPredicate",
              "layer": "#/$defs/LayerPredicate",
              "path": "#/$defs/PathPredicate",
              "property": "#/$defs/PropertyPredicate",
              "tag": "#/$defs/TagPredicate",
              "text": "#/$defs/TextPredicate",
              "withinHops": "#/$defs/WithinHopsPredicate"
            },
            "propertyName": "kind"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/TextPredicate"
            },
            {
              "$ref": "#/$defs/PropertyPredicate"
            },
            {
              "$ref": "#/$defs/TagPredicate"
            },
            {
              "$ref": "#/$defs/HasPropertyPredicate"
            },
            {
              "$ref": "#/$defs/DescendantOfPredicate"
            },
            {
              "$ref": "#/$defs/WithinHopsPredicate"
            },
            {
              "$ref": "#/$defs/EntityTypePredicate"
            },
            {
              "$ref": "#/$defs/LayerPredicate"
            },
            {
              "$ref": "#/$defs/DegreePredicate"
            },
            {
              "$ref": "#/$defs/IsOrphanPredicate"
            },
            {
              "$ref": "#/$defs/IsLeafPredicate"
            },
            {
              "$ref": "#/$defs/IsRootPredicate"
            },
            {
              "$ref": "#/$defs/HasIncomingPredicate"
            },
            {
              "$ref": "#/$defs/HasOutgoingPredicate"
            },
            {
              "$ref": "#/$defs/PathPredicate"
            },
            {
              "$ref": "#/$defs/GroupPredicate"
            }
          ],
          "title": "Predicate"
        },
        "scope": {
          "$ref": "#/$defs/SearchScope"
        }
      },
      "required": [
        "predicate",
        "scope"
      ],
      "title": "SearchQuery",
      "type": "object"
    },
    "SearchResultPage": {
      "description": "Provider + service response. One inner list in ``aggregates`` per\nrequested AggregationSpec.",
      "properties": {
        "aggregates": {
          "anyOf": [
            {
              "items": {
                "items": {
                  "$ref": "#/$defs/SearchAggregateBucket"
                },
                "type": "array"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Aggregates"
        },
        "cacheHit": {
          "default": false,
          "title": "Cachehit",
          "type": "boolean"
        },
        "candidateCount": {
          "default": 0,
          "description": "Candidates that passed the predicate scan before the scope check and aggregation/limit. Useful for showing 'searching X nodes\u2026' captions in the FE.",
          "title": "Candidatecount",
          "type": "integer"
        },
        "cursor": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Cursor"
        },
        "deadlineExceeded": {
          "default": false,
          "title": "Deadlineexceeded",
          "type": "boolean"
        },
        "elapsedMs": {
          "title": "Elapsedms",
          "type": "integer"
        },
        "hits": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/SearchHit"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Ordered by server relevance; do not re-sort by `score`. Ranking runs over the whole candidate set before this page is sliced from it, so `score` is a per-hit annotation, not the key the list is in.",
          "title": "Hits"
        },
        "paths": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/PathHit"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Populated when the request's predicate contains a PathPredicate and options.results='paths'. Ordered node\u2192edge\u2192node sequences from source to target.",
          "title": "Paths"
        },
        "queryExplain": {
          "anyOf": [
            {
              "$ref": "#/$defs/QueryExplain"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "scopeDiagnostics": {
          "anyOf": [
            {
              "$ref": "#/$defs/ScopeDiagnostics"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Resolved-scope + ontology diagnostics. Surfaced on every response so the FE can interpret 0-result cases without round-tripping to /search/explain."
        },
        "totalCount": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exact number of matches in scope, independent of the candidate cap; null when the count timed out (UI shows N+).",
          "title": "Totalcount"
        },
        "truncated": {
          "default": false,
          "description": "True when the provider hit its candidate cap or soft deadline before exhausting the candidate set.",
          "title": "Truncated",
          "type": "boolean"
        }
      },
      "required": [
        "elapsedMs"
      ],
      "title": "SearchResultPage",
      "type": "object"
    },
    "SearchScope": {
      "description": "Bounds the search.\n\n``view_id`` is **required** \u2014 every search must be bound to a view\n(for security + telemetry), but the *scope* of the search is now\ncontrolled by ``scope_mode``:\n\n  * ``visible`` (default UI mode): only the URNs the user can\n    actually see right now on the canvas (passed in\n    ``visible_urns``). This is the \"fast feedback\" mode that\n    matches the user's mental model of \"search what's in front of\n    me.\" Falls back to ``view`` mode when ``visible_urns`` is\n    empty.\n\n  * ``view``: classic behaviour \u2014 the view's authorised roots,\n    expanded via containment, then narrowed by client hints.\n\n  * ``data_source``: power-user override. Searches the entire data\n    source (no view containment clamp). Results may include URNs\n    that are not in this view; the FE surfaces a disclaimer.\n\n``root_urns`` / ``entity_types`` / ``layer_assignment`` / ``max_depth``\nare **narrowing hints** that always apply (regardless of mode).\nThe resolver intersects them with the view's authorised scope.",
      "properties": {
        "entityTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 512,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional. Must be \u2286 view's visibleEntityTypes; out-of-set values cause the request to be rejected with 400.",
          "title": "Entitytypes"
        },
        "layerAssignment": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Layerassignment"
        },
        "maxDepth": {
          "anyOf": [
            {
              "maximum": 20,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 12,
          "description": "Clamped to min(client, view.maxDepth) by the resolver.",
          "title": "Maxdepth"
        },
        "rootUrns": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional narrowing hint. Each URN must be a descendant of (or equal to) one of the view's allowed roots; URNs that fail validation are dropped server-side. Capped at DEEP_SEARCH_SCOPE_ROOT_URNS_CAP entries (default 256). The cap exists to bound the Cypher IN-list size + containment expansion fanout on multi-domain views with many top-level containers.",
          "title": "Rooturns"
        },
        "scopeMode": {
          "default": "view",
          "description": "visible: filter to URNs in scope.visible_urns (fast in-view search). view: view's authorised roots + containment expansion (default for server-direct callers). data_source: entire data source \u2014 power-user override.",
          "enum": [
            "visible",
            "view",
            "data_source"
          ],
          "title": "Scopemode",
          "type": "string"
        },
        "viewId": {
          "description": "Required. The view the search is bound to (security context + telemetry). Scope behaviour depends on ``scope_mode``.",
          "minLength": 1,
          "title": "Viewid",
          "type": "string"
        },
        "visibleUrns": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "maxItems": 20000,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "URNs currently rendered in the canvas. Required when scope_mode='visible'. Ignored in other modes. Capped at DEEP_SEARCH_VISIBLE_URNS_CAP entries (default 20000).",
          "title": "Visibleurns"
        }
      },
      "required": [
        "viewId"
      ],
      "title": "SearchScope",
      "type": "object"
    },
    "TagPredicate": {
      "description": "Match against the (currently JSON-stringified) ``n.tags`` field.\n\nTags remain stringified in this workstream \u2014 see the storage-refactor\ncommit. Server-side this maps to ``n.tags CONTAINS $tag`` per value,\ncomposed by ``op``.",
      "properties": {
        "kind": {
          "const": "tag",
          "default": "tag",
          "title": "Kind",
          "type": "string"
        },
        "op": {
          "default": "has",
          "enum": [
            "has",
            "hasAll",
            "hasAny",
            "notHas"
          ],
          "title": "Op",
          "type": "string"
        },
        "values": {
          "items": {
            "type": "string"
          },
          "maxItems": 32,
          "minItems": 1,
          "title": "Values",
          "type": "array"
        }
      },
      "required": [
        "values"
      ],
      "title": "TagPredicate",
      "type": "object"
    },
    "TextPredicate": {
      "description": "Free-form text match against a single field (or ``any``).",
      "properties": {
        "boost": {
          "default": 1.0,
          "description": "Relevance multiplier when match='fulltext'.",
          "maximum": 10.0,
          "minimum": 0.0,
          "title": "Boost",
          "type": "number"
        },
        "caseSensitive": {
          "default": false,
          "title": "Casesensitive",
          "type": "boolean"
        },
        "kind": {
          "const": "text",
          "default": "text",
          "title": "Kind",
          "type": "string"
        },
        "match": {
          "default": "substring",
          "enum": [
            "exact",
            "prefix",
            "suffix",
            "substring",
            "fulltext",
            "regex"
          ],
          "title": "Match",
          "type": "string"
        },
        "propertyKey": {
          "anyOf": [
            {
              "maxLength": 128,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Required when ``target='property'``; ignored otherwise.",
          "title": "Propertykey"
        },
        "target": {
          "default": "any",
          "enum": [
            "name",
            "qualifiedName",
            "description",
            "tags",
            "property",
            "any"
          ],
          "title": "Target",
          "type": "string"
        },
        "value": {
          "maxLength": 512,
          "minLength": 1,
          "title": "Value",
          "type": "string"
        }
      },
      "required": [
        "value"
      ],
      "title": "TextPredicate",
      "type": "object"
    },
    "WithinHopsPredicate": {
      "description": "Match nodes within N relationship hops of any anchor URN.\n\nPowers lineage-aware questions (\"any column 2 hops downstream of\n``orders.id``\"). ``edge_types`` restricts the traversal to specific\nrelationship types; omitted means all. ``direction='out'`` walks\nout-edges only, ``'in'`` walks in-edges, ``'both'`` is undirected.",
      "properties": {
        "direction": {
          "default": "both",
          "enum": [
            "out",
            "in",
            "both"
          ],
          "title": "Direction",
          "type": "string"
        },
        "edgeClass": {
          "default": "lineage",
          "description": "Default edge-class when ``edge_types`` is omitted. Mirrors DegreePredicate / PathPredicate.",
          "enum": [
            "lineage",
            "containment",
            "any"
          ],
          "title": "Edgeclass",
          "type": "string"
        },
        "edgePredicate": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "edgeGroup": "#/$defs/EdgeGroupPredicate",
                  "edgeHasProperty": "#/$defs/EdgeHasPropertyPredicate",
                  "edgeProperty": "#/$defs/EdgePropertyPredicate"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/EdgePropertyPredicate"
                },
                {
                  "$ref": "#/$defs/EdgeHasPropertyPredicate"
                },
                {
                  "$ref": "#/$defs/EdgeGroupPredicate"
                }
              ]
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional predicate evaluated against every traversed edge. ANDs with ``edge_types`` / ``edge_class``. Compiles to ``ALL(rel IN relationships(p) WHERE \u2026)``.",
          "title": "Edgepredicate"
        },
        "edgeTypes": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Edgetypes"
        },
        "hops": {
          "maximum": 10,
          "minimum": 1,
          "title": "Hops",
          "type": "integer"
        },
        "kind": {
          "const": "withinHops",
          "default": "withinHops",
          "title": "Kind",
          "type": "string"
        },
        "urns": {
          "items": {
            "type": "string"
          },
          "maxItems": 64,
          "minItems": 1,
          "title": "Urns",
          "type": "array"
        }
      },
      "required": [
        "urns",
        "hops"
      ],
      "title": "WithinHopsPredicate",
      "type": "object"
    }
  },
  "description": "Bundle root that wraps every API-surface shape in one model.\n\nExists purely as a code-generation seed: ``model_json_schema()`` on\nthis class produces a single JSON Schema document whose ``$defs``\ncontain every type the FE / AI agents need. The frontend's\n``gen:search-schema`` script feeds this schema into\n``json-schema-to-typescript`` to produce the canonical TS types.\n\nNot used at runtime \u2014 no endpoint sends or receives this shape.\nFields are kept ``Optional`` so a default constructor produces a\nvalid (empty) instance for tooling that needs one.",
  "properties": {
    "scopeDiagnostics": {
      "anyOf": [
        {
          "$ref": "#/$defs/ScopeDiagnostics"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "searchDiscoverResult": {
      "anyOf": [
        {
          "$ref": "#/$defs/SearchDiscoverResult"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "searchExplainResult": {
      "anyOf": [
        {
          "$ref": "#/$defs/SearchExplainResult"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "searchQuery": {
      "anyOf": [
        {
          "$ref": "#/$defs/SearchQuery"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "searchResultPage": {
      "anyOf": [
        {
          "$ref": "#/$defs/SearchResultPage"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "title": "SearchApiContract",
  "type": "object"
}
