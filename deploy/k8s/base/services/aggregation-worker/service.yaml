apiVersion: v1
kind: Service
metadata:
  name: aggregation-worker
  namespace: synodic
  labels:
    app.kubernetes.io/name: aggregation-worker
spec:
  clusterIP: None
  ports:
    - port: 8090
      targetPort: 8090
      name: http
  selector:
    app.kubernetes.io/name: aggregation-worker
