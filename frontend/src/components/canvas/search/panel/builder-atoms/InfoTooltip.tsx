/**
 * Moved to `@/components/ui/InfoTooltip` — it is no longer a search-panel atom.
 * Re-exported here so the existing builder-atom imports keep working.
 */
export { InfoTooltip, type InfoTooltipProps } from '@/components/ui/InfoTooltip'
