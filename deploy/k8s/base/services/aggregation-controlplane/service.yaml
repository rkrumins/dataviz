apiVersion: v1
kind: Service
metadata:
  name: aggregation-controlplane
  namespace: synodic
  labels:
    app.kubernetes.io/name: aggregation-controlplane
spec:
  type: ClusterIP
  ports:
    - port: 8091
      targetPort: 8091
      name: http
  selector:
    app.kubernetes.io/name: aggregation-controlplane
