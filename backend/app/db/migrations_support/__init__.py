"""Pure, dependency-light helpers imported by alembic data migrations.

Modules here contain no SQLAlchemy / ORM / DB code — only pure dict logic —
so they are safe to import from an alembic revision file and unit-testable
in isolation.
"""
