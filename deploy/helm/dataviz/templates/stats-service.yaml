apiVersion: apps/v1
kind: Deployment
metadata:
  name: stats-service
  labels:
    {{- include "dataviz.labels" . | nindent 4 }}
    app.kubernetes.io/name: stats-service
spec:
  replicas: {{ .Values.services.stats.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: stats-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stats-service
        app.kubernetes.io/part-of: dataviz
    spec:
      terminationGracePeriodSeconds: {{ .Values.services.stats.terminationGracePeriodSeconds }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.ordering.enabled (not .Values.services.stats.skipSchemaCheck) }}
      initContainers:
        {{- if .Values.ordering.enabled }}
        {{- include "dataviz.waitForControlplane" . | nindent 8 }}
        {{- end }}
        {{- if not .Values.services.stats.skipSchemaCheck }}
        {{- include "dataviz.schemaCheckInitContainer" (dict "ctx" $) | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        - name: stats-service
          image: {{ include "dataviz.image" (dict "ctx" $ "name" "stats-service") | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: health
              containerPort: 8092
          envFrom:
            - configMapRef:
                name: dataviz-config
            - secretRef:
                name: {{ include "dataviz.secretName" . }}
          env:
            - name: SYNODIC_ROLE
              value: "stats"
            # Right-size the Postgres pools for a concurrency-2 worker —
            # the engine defaults are web-tier-tuned (~27 peak conns).
            - name: DB_JOBS_POOL_SIZE
              value: "4"
            - name: DB_JOBS_POOL_MAX_OVERFLOW
              value: "2"
            - name: DB_READONLY_POOL_SIZE
              value: "3"
            - name: DB_READONLY_POOL_MAX_OVERFLOW
              value: "2"
          startupProbe:
            httpGet:
              path: /health
              port: 8092
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /health
              port: 8092
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8092
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.services.stats.resources | nindent 12 }}
