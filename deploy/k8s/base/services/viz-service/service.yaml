apiVersion: v1
kind: Service
metadata:
  name: viz-service
  namespace: synodic
  labels:
    app.kubernetes.io/name: viz-service
  annotations:
    cloud.google.com/backend-config: '{"default": "viz-service-backendconfig"}'
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app.kubernetes.io/name: viz-service
